{
  "installCommand": "npm ci",
  "buildCommand": "if [ -f client/package.json ]; then npm run build --workspace client && rm -rf dist && cp -R client/dist dist; else npm run build; fi",
  "outputDirectory": "dist",
  "functions": {
    "api/[...path].ts": {
      "runtime": "nodejs20.x"
    }
  },
  "rewrites": [
    {
      "source": "/((?!api/|assets/|.*\\..*).*)",
      "destination": "/index.html"
    }
  ]
}
