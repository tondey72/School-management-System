{
  "installCommand": "npm ci",
  "buildCommand": "npm run prisma:generate --workspace server && if [ -f client/package.json ]; then npm run build --workspace client && rm -rf dist && cp -R client/dist dist; else npm run build; fi",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api?path=$1"
    },
    {
      "source": "/((?!api/|assets/|.*\\..*).*)",
      "destination": "/index.html"
    }
  ]
}
